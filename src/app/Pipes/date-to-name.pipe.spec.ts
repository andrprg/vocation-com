import { DateToNamePipe } from './date-to-name.pipe';

describe('DateToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DateToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
